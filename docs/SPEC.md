# Exam System - 宪法 (Constitution)

> 本文档是系统的最高设计准则。任何实现如果与本文冲突，以本文为准。
> 所有 agent session 开始前必须理解本文内容。

---

## 一、系统定位

**通用型内网考试平台。**

部署在校园/机构内网（LAN），服务多种考试场景：实验室准入考试、学院考试、培训确认测验、闭卷期末考试等。核心逻辑——"考完出分，达标放行"。

各院系/实验室/部门作为租户接入，各自管理题库和考试。考生不限于学生——由各机构自己定义考生身份。

---

## 二、核心领域模型

### 2.1 实体关系

```
Organization (机构/租户)
 ├── 支持平铺（多个独立机构）和树形层级（大学→学院→实验室）
 ├── 每个租户有自己的 Admin、题库、考试、考生字段定义
 └── 数据隔离：A 机构看不到 B 机构的题目和成绩

SuperAdmin (平台管理员)
 └── 跨租户管理所有机构

Organization 内部：
 User (用户)
  ├── Admin    → 管理本机构、用户管理、考场管理、考生字段配置
  ├── Teacher  → 出题、组卷、审卷、查看成绩
  ├── Proctor  → 监考（查看考场状态、处理异常、标记违纪）
  │             注：教师可以兼任监考，监考权限是教师权限的子集
  └── Candidate → 参加考试（通用身份，不绑定"学生"语义）
                  考生属性由机构自定义（学号/工号/身份证号等）
                  内部 ID 用 UUID，用户可见标识由机构定义

Course (课程/科目)
 └── 属于某个 Organization

QuestionBank (题库)
 └── 按 Course 组织（各机构题库隔离）
      └── Question (题目)
           ├── type: single_choice | multiple_choice | fill_blank | true_false
           ├── content: 题干（支持文字 + 图片引用）
           ├── attachments[]: 图片/附件 URL
           ├── options[]: 选项（选择题）
           ├── standardAnswer: 标准答案（自动批改的依据）
           ├── score: 分值
           └── tags[]: 标签（知识点、难度）

Exam (考试)
 ├── 从 QuestionBank 中抽题组卷
 ├── passingScore: 及格线
 ├── timing: timed_sync | timed_window | deadline | untimed（见 §2.4）
 ├── durationMinutes: 考试时长（timed 模式必填）
 ├── 管控细项（每项可独立开关，见 §2.5）
 └── startTime / endTime: 开放时间窗口

ExamRoom (考场)
 ├── name: 考场名称
 ├── ipRange: 允许的 IP 段（LAN 安全）
 └── capacity: 容量

ExamPaper (答卷)
 ├── 某考生 × 某考试 = 一份答卷
 ├── questionSnapshot: 题目快照（组卷时固定，不随题库修改变化）
 ├── answers: 考生的作答
 ├── score: 得分
 ├── passed: 是否通过
 └── lastActivityAt: 心跳时间（断线检测）
```

### 2.2 考生模型

系统不预设"考生是学生"——每个机构自己定义考生的身份字段。

```
化学学院可能定义：
  - 姓名（必填）、学号（必填，唯一标识）、班级、实验室

安全培训部可能定义：
  - 姓名（必填）、工号（必填，唯一标识）、部门、手机号

外部培训机构可能定义：
  - 姓名（必填）、身份证号（必填，唯一标识）、单位
```

**CandidateField（考生字段模板）**：
- 每个 Organization 可自定义 N 个考生字段
- 每个字段可配置：名称、类型（text/number/select）、是否必填、是否作为唯一标识
- 考生导入模板根据字段定义动态生成
- 成绩导出时按该机构的字段输出对应列

**标识规则**：
- 系统内部 ID 统一用 UUID（`c_01HXYZ...`）
- 用户可见的"考号/学号/工号"是机构自定义的唯一标识字段
- 标识可以是数字、字母、混合（如 `s2024001`、`b2024001`、`2024010001`）
- 系统不假设标识的格式，只保证唯一性

### 2.3 角色权限矩阵

| 能力 | SuperAdmin | Admin | Teacher | Proctor | Candidate |
|------|:----------:|:-----:|:-------:|:-------:|:---------:|
| 跨租户管理 | ✅ | - | - | - | - |
| 机构设置 / 考生字段 | - | ✅ | - | - | - |
| 用户管理 | - | ✅ | - | - | - |
| 考场管理 | - | ✅ | - | - | - |
| 出题 / 题库 | - | ✅ | ✅ | - | - |
| 组卷 / 发布考试 | - | ✅ | ✅ | - | - |
| 查看所有成绩 | - | ✅ | ✅ | - | - |
| 监考：查看考场实时状态 | - | ✅ | ✅ | ✅ | - |
| 监考：延长个人时间 | - | ✅ | ✅ | ✅ | - |
| 监考：标记违纪 | - | ✅ | ✅ | ✅ | - |
| 监考：强制交卷 | - | ✅ | ✅ | ✅ | - |
| 参加考试 | - | - | - | - | ✅ |
| 查看自己成绩 | - | - | - | - | ✅ |

### 2.4 考试计时模式

四种计时方式，覆盖从"严格统考"到"随到随考"的全部场景：

| 模式 | 时间规则 | 典型场景 |
|------|----------|----------|
| **定时统考** `timed_sync` | 监考员统一触发开考，所有人同时开始倒计时，到时强制交卷 | 期末考试、软考机考 |
| **窗口限时** `timed_window` | 在开放窗口内考生自选时间开始，开始后倒计时 | 实验室准入、随堂测验 |
| **纯截止日** `deadline` | 只有截止时间，不计时，做完就交 | 培训确认、课后作业 |
| **不限时** `untimed` | 永久开放，随时做随时交（或管理员手动关闭） | 练习题、模拟考试 |

```
timed_sync 示例：
  Proctor 点击"开考" → 考生排队分批进入 → 开始 90 分钟倒计时
  窗口：周一 9:00-11:00，最迟 10:30 可开始（预留 buffer）

timed_window 示例：
  开放窗口：周一到周五
  考生周三 14:00 点"开始" → 开始 60 分钟倒计时
  周五 23:59 窗口关闭，未开始的不能再进入

deadline 示例：
  截止时间：本周日 23:59
  不计时，做完点交卷，没做的话到截止日自动提交

untimed 示例：
  长期开放的练习题，不限时
  管理员可随时手动关闭
```

### 2.5 管控与考试模式

管控强度独立于计时方式，两者正交组合。Teacher 组卷时先选计时方式，再逐项勾选管控选项。系统提供"开卷预设"和"闭卷预设"快速填充默认值，但每一项都能单独改。

| 管控项 | 开卷预设 | 闭卷预设 | 说明 |
|--------|:--------:|:--------:|------|
| `shuffleQuestions` | 关 | 开 | 题目乱序 |
| `shuffleOptions` | 关 | 开 | 选项乱序 |
| `detectTabSwitch` | 关 | 开 | 切屏检测（Phase 1 仅记录+报告监考员） |
| `disableCopyPaste` | 关 | 开 | 前端禁用右键/选择/复制 |
| `requireQueue` | 关 | 开 | 排队分批进入 |
| `batchSize` | - | 10 | 每批放行人数 |
| `batchInterval` | - | 3 | 批次间隔秒数 |
| `restrictIp` | 关 | 开 | 仅允许考场 IP 段 |
| `requireLockdown` | 关 | 关（Phase 2） | 强制 Electron 锁屏 |
| `showResultImmediately` | 开 | 可配置 | 交卷后是否立即显示成绩 |
| `retakePolicy` | unlimited | max_attempts | 重考策略 |
| `maxRetakeAttempts` | - | 1 | 最大重考次数 |
| `retakeCooldown` | 0 | 60 | 重考冷却时间（分钟） |
| `scoreStrategy` | highest | latest | 多次考试取分策略 |
| `passThenStop` | 开 | 关 | 通过后不能再考 |

### 2.6 考试次数限制

| retakePolicy | 说明 |
|-------------|------|
| `unlimited` | 不限次数 |
| `max_attempts` | 最多 N 次 |
| `daily_limit` | 每天最多 N 次 |
| `weekly_limit` | 每周最多 N 次 |
| `pass_then_stop` | 通过后不能再考 |

| scoreStrategy | 说明 |
|--------------|------|
| `highest` | 取最高分（默认） |
| `latest` | 取最近一次 |
| `first` | 取第一次 |

多次考试的成绩记录都保留，只是最终"认定"的成绩按策略选。

### 2.7 多租户与"达标放行"

```
Organization (机构/租户)
  ├── 拥有自己的 Admin、Teacher、题库、考试、考生字段定义
  ├── 数据隔离：A 机构看不到 B 机构的题目和成绩
  ├── 支持平铺 + 树形层级（大学→学院→实验室，上级可看下级）
  └── SuperAdmin 跨租户管理

"达标放行"流程：
  考试发布 → 考生参加 → 自动批改 → score >= passingScore → passed=true
       ↓
  外部系统可通过 API 查询"某人是否通过了某考试"
  （如：实验室门禁系统调接口确认已通过安全考试才放行）
```

租户隔离规则：
- 数据库层面：所有表都有 `organizationId` 字段，查询时强制过滤
- 一个 Candidate 可以属于多个 Organization
- 树形层级中，上级 Organization 的 Admin 可查看下级数据

### 2.8 客户端架构

系统提供两种客户端，共享同一套 React 代码：

| 客户端 | 场景 | 特性 |
|--------|------|------|
| **Web（浏览器）** | Teacher/Proctor/Admin 后台、开卷考试、Phase 1 全场景 | 标准浏览器 |
| **Electron 桌面端** | 闭卷考试锁屏（Phase 2） | 禁止切应用、全屏强制、设备指纹 |

```
exam/
├── client/         # 共享 React 代码（Web 版直接用）
├── desktop/        # Electron 壳（Phase 2）
└── server/         # 后端 API
```

---

## 三、核心工作流

### 3.1 出题与题库管理

**题目导入格式**：

| 格式 | 用途 | 说明 |
|------|------|------|
| Excel (.xlsx) | 批量导入 | 固定模板：题型、题干、选项A-D、标准答案、分值、标签 |
| CSV | 批量导入 | 同 Excel 逻辑的纯文本版本 |
| JSON | API/程序导入 | 完整 Question 结构，供脚本调用 |
| 手动录入 | 单题创建 | Web 端表单，支持预览 |

导入流程：
1. 上传文件 → 服务端解析校验 → 预览（展示识别出的题目，标记异常行）
2. Teacher 确认 → 批量写入 QuestionBank
3. 校验规则：题型必填、选择题必须有选项和标准答案、分值 > 0
4. 重复检测：基于题干相似度标记，不阻断

**题目内容**：
- 题干支持文字 + 图片引用（图片 URL）
- Phase 1 不做 LaTeX / 公式编辑器，纯文本 + 图片
- 简答题/论文/画图等主观题型留 Phase 2

### 3.2 组卷

```
Teacher 新建 Exam → 选择 Course
       ↓
选题方式：手动选题 / 随机抽题（按规则：题型、难度、标签分布）
       ↓
配置计时方式（§2.4）+ 管控选项（§2.5）
       ↓
预览考试（查看题目、配置确认）
       ↓
发布 → 状态变为 Published
```

### 3.3 考生管理

**考生信息导入**：

| 方式 | 说明 |
|------|------|
| Excel/CSV | 下载动态模板（根据机构自定义的考生字段生成列头），填写后上传 |
| 手动录入 | Web 端表单，按机构字段动态渲染 |
| API | 供教务系统/外部系统自动同步 |
| CAS/OAuth | 统一身份认证自动拉取（Phase 2） |

导入流程：
1. Admin 配置本机构的 CandidateField（定义有哪些字段、哪些必填、哪个是唯一标识）
2. 下载导入模板（列头 = 已定义的字段名）
3. 填写上传 → 校验（必填项、唯一标识重复检测）
4. 确认入库
5. 同一唯一标识重复导入时：更新已有信息，不创建重复记录

### 3.4 考试执行

**考生答题界面**（参考软考机考设计）：

```
┌─────────────────────────────────────────────────────┐
│  考试名称  │  剩余时间: 78:32  │  已答 15/50  │ 交卷 │
├──────────┬──────────────────────────────────────────┤
│ 题号导航  │  题目区域                                │
│          │                                          │
│ ① ●     │  16. 以下关于化学实验安全...             │
│ ② ●     │                                          │
│ ③ ○     │  A. 选项一                               │
│ ...      │  B. 选项二  ← 当前选中                   │
│ 16 ←    │  C. 选项三                               │
│ 17 ○    │  D. 选项四                               │
│ ...      │                                          │
│ 50 ○    │  ◀ 上一题    标记本题    下一题 ▶         │
│          │                                          │
│ ●=已答   │                                          │
│ ○=未答   │                                          │
│ ◉=已标记 │                                          │
├──────────┴──────────────────────────────────────────┤
│  已答: 15  │  未答: 35  │  已标记: 3  │  总分: 100   │
└─────────────────────────────────────────────────────┘
```

核心交互：
- 题号导航栏：颜色标记状态（已答/未答/已标记），可点击跳转
- 标记功能：对不确定的题打标记，交卷前可查看标记列表
- 自动保存：每次选择/输入后自动保存，已保存题目标记 ✓
- 交卷确认：弹出确认框，显示未答和已标记题数

**答题组件**（Phase 1 仅客观题）：

| 题型 | 答题组件 | 说明 |
|------|----------|------|
| 单选 | Radio 按钮组 | 点选即保存 |
| 多选 | Checkbox 组 | 每次变更实时保存 |
| 判断 | 二选一 Radio | 同单选 |
| 填空 | 文本输入框（支持多个空） | 每个空独立，blur 时保存 |

**排队分批进入**（用于闭卷统考）：

```
Proctor 点击"开考"
      ↓
考生端显示："考试即将开始，排队中... 前面还有 X 人"
      ↓
每批放行 N 人（batchSize 配置，默认 10），间隔 3-5 秒
      ↓
轮到时自动跳转到考试页面，倒计时开始
```

### 3.5 监考端

**监考面板**：

```
┌──────────────────────────────────────────────────────┐
│  考场: 机房A201  │  考试: 化学安全测验  │  在线: 28/30 │
├──────────┬───────────────────────────────────────────┤
│ 考生列表  │  考场实时状态                              │
│          │                                           │
│ ● 张三   │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐       │
│ ● 李四   │  │ 01  │ │ 02  │ │ 03  │ │ 04  │ ...   │
│ ◌ 王五   │  │ 正常 │ │ 正常 │ │中断!│ │ 正常 │       │
│ ⚠ 孙七   │  └─────┘ └─────┘ └─────┘ └─────┘       │
│          │                                           │
│ 在线:28  │  实时事件流:                                │
│ 断线:1   │  09:32 王五 连接中断                         │
│ 异常:1   │  09:30 孙七 切屏 3 次 (标记异常)            │
│          │  09:28 李四 已交卷 (得分: 92 通过)           │
├──────────┴───────────────────────────────────────────┤
│  [广播通知]  [批量延时]  [结束本场考试]                    │
└──────────────────────────────────────────────────────┘
```

监考能力：

| 功能 | 说明 | 实时性 |
|------|------|--------|
| 考场总览 | 在线/断线/异常/已交卷人数 | WebSocket / 轮询 |
| 考生状态卡片 | 每人进度（已答 N/M）、剩余时间、连接状态 | WebSocket / 轮询 |
| 座位图视图 | 按座位排列，颜色标记状态 | WebSocket / 轮询 |
| 断线告警 | 心跳超时自动高亮 + 声音提示 | 即时 |
| 切屏记录 | 超阈值标红（Phase 1 仅记录，不阻断） | 即时 |
| 实时事件流 | 登录、交卷、断线、异常、重连的时间线 | 推送 |
| 单人操作 | 延长某人时间 / 强制交卷 / 标记违纪 | 按钮 |
| 批量操作 | 批量延时 / 广播通知 / 结束本场 | 按钮 |
| 违纪记录 | 标记原因，写入 AuditLog，可导出 | 持久化 |

### 3.6 考试状态持久化与容灾

> 考试进行中的数据是最珍贵的数据——已作答的内容不能丢。

**答案保存策略**：

| 层级 | 触发时机 | 说明 |
|------|----------|------|
| 客户端内存 | 每次按键/选择 | 即时响应 |
| 服务端持久化 | 每次作答变更（去抖 1-2s）或每 30s 自动保存 | 已保存题目标记 ✓ |
| 客户端本地缓存 | 每次保存时同步写入 localStorage / IndexedDB | 断网兜底 |

**宕机恢复**：

| 场景 | 恢复方式 |
|------|----------|
| 客户端崩溃 | 重新登录 → 服务端找到 in_progress 的 ExamPaper → 恢复答案 + 剩余时间 |
| 服务端重启 | 答案已实时持久化到数据库 → 所有 in_progress 试卷保持原状态 → 考生继续 |
| 网络中断 | 客户端切入离线模式 → 答案暂存本地 → 恢复后批量同步（按时间戳去重） |

**ExamPaper 状态机**：

```
not_started → in_progress → submitted → graded
                  ↑              
                  └── disrupted (心跳超时自动标记，Proctor 可恢复)
```

- `disrupted`：客户端心跳超时（60s 无心跳）自动标记
- `lastActivityAt`：心跳字段，服务端用它检测断线
- Proctor 可手动将 `disrupted` 恢复为 `in_progress`
- 离线期间计时不停（服务端时间是权威）

### 3.7 批改

**Phase 1：自动批改**

| 题型 | 批改规则 |
|------|----------|
| 单选 / 判断 | 精确匹配标准答案 |
| 多选 | 全对满分，少选半分，错选零分（可配置） |
| 填空 | 精确匹配或关键词匹配（可配置模糊度） |

**Phase 2：AI 辅助**
- 短答题 / 简答题：本地部署 LLM 语义批改
- AI 批改结果可被 Teacher 覆盖
- 不依赖外部 API

### 3.8 数据导入导出

**考生信息导入**（见 §3.3）

**数据导出**：

| 导出类型 | Excel | CSV | PDF | Word | JSON |
|----------|:-----:|:---:|:---:|:----:|:----:|
| 成绩单（原始数据） | ✅ | ✅ | - | - | ✅ |
| 成绩单（正式打印） | - | - | ✅ | - | - |
| 答卷详情 | ✅ | - | ✅ | - | - |
| 统计分析报告 | ✅ | - | ✅ | ✅ | - |
| 题库备份 | ✅ | - | - | - | ✅ |
| 考生名单 | ✅ | ✅ | - | - | ✅ |
| 审计日志 | - | ✅ | - | - | ✅ |
| 达标证明（单人） | - | - | ✅ | - | - |

导出原则：
- 成绩单列头按该机构的 CandidateField 动态生成（不是固定输出"学号"）
- 所有导出操作写入 AuditLog
- 大批量导出异步处理（生成后通知下载）
- 导出文件名含时间戳和考试名称
- 可定制 PDF/Word 模板（占位符替换，Phase 1 先做默认模板）

---

## 四、系统架构

### 4.1 技术栈

| 层 | 技术 |
|----|------|
| 前端 | React 19 + Vite + TypeScript |
| UI | shadcn/ui + TailwindCSS v4 |
| 桌面端 | Electron（Phase 2，锁屏客户端） |
| 后端 | Bun + ElysiaJS + TypeBox 校验 |
| 数据库 | SQLite (dev) / PostgreSQL (prod)，Drizzle ORM 抽象 |
| 认证 | @elysiajs/jwt, HTTP-only cookies, Bun.password (argon2id) |
| 实时通信 | WebSocket（正常模式）/ 长轮询（降级模式） |
| 包管理 | Bun workspaces |

### 4.2 自适应降级

系统持续自检（CPU / 内存 / DB 响应时间），三档自动切换：

| 档位 | 触发条件 | 行为 |
|------|----------|------|
| **正常** | 资源充裕 | WebSocket 推送、答案即时持久化、统计即时算 |
| **省电** | CPU>70% 或 内存>80% 或 DB>500ms | 监考面板降级为轮询(5s)、答案批量写入、心跳间隔拉长(30s→60s) |
| **极限** | CPU>90% 或 内存>95% 或 DB>2s | 仅核心考试 API、监考面板手动刷新、答案攒批(最多延迟10s)、非考试请求限流 |

关键原则：
- 降级不丢数据——答案仍然保证持久化，只是攒批写入
- 回弹要缓——必须先经过省电模式稳定一段时间
- 考生端无感——答题界面不受影响
- 三档通过 `.env` 配置阈值

**网络降级**：

| 层级 | 策略 |
|------|------|
| 正常 | WebSocket 双向通信 |
| 网络抖动 | 自动降级为长轮询(3s)，WebSocket 自动重连 |
| 断网 | 客户端离线模式，答案存 localStorage + IndexedDB |
| 恢复 | 自动重连，离线答案按时间戳批量同步去重 |

**客户端状态感知**：
- 考生端：连接状态指示（绿=已同步 / 黄=同步中 / 红=离线模式）
- 监考端：顶栏显示当前系统档位（正常/省电/极限）
- Admin 端：系统健康仪表盘（CPU/内存/连接数/DB响应），可手动切换档位

### 4.3 项目结构

```
exam/
├── client/             # React + Vite + shadcn/ui
│   └── src/
│       ├── components/ui/   # shadcn 组件（自动生成，勿手改）
│       ├── pages/           # 路由级组件
│       ├── lib/             # 工具函数、API client
│       └── hooks/           # 共享 React hooks
├── server/             # Bun + ElysiaJS
│   └── src/
│       ├── routes/          # 路由处理器（一个文件一个领域）
│       ├── middleware/       # auth、rate-limit、exam-session guards
│       ├── plugins/         # Elysia 插件（CORS、JWT、安全头）
│       └── db/              # Drizzle schema、migrations、连接
├── shared/             # 共享类型
│   └── src/
│       └── types.ts         # 领域类型（唯一数据源）
├── desktop/            # Electron 壳（Phase 2）
└── docs/               # 设计文档
```

### 4.4 部署方案

| 场景 | 方式 | 数据库 | 说明 |
|------|------|--------|------|
| 开发演示 | `bun run dev` | SQLite | 零配置，单命令启动 |
| 低配生产 | Docker 单容器 | SQLite | 适合 2G 内存机器，极限模式兜底 |
| 正式生产 | Docker Compose | PostgreSQL | 前后端 + DB 一键部署 |
| 手动部署 | systemd + Nginx | PostgreSQL | 不用 Docker，传统方式 |

**Docker Compose（正式生产）**：

```yaml
services:
  app:        # Bun server（API + 静态文件）
  db:         # PostgreSQL
  # 可选：
  # nginx:    # 反向代理 + 静态文件缓存
```

**Docker 单容器（低配）**：

```yaml
services:
  app:        # Bun server + SQLite
```

通过 `.env` 配置切换：
- `DATABASE_URL` 指向 PostgreSQL 或 SQLite 文件路径
- `DEGRADATION_THRESHOLDS` 调整降级阈值
- `AUTH_MODE` 选择认证方式（local / cas / oauth）

---

## 五、安全原则

> 考试系统的安全不是锦上添花，是核心需求。

### 5.1 认证与授权

- JWT + HTTP-only Cookie，不使用 localStorage 存 token
- 五种角色（SuperAdmin / Admin / Teacher / Proctor / Candidate），API 按角色鉴权
- 教师可兼任监考
- 考试期间一个考生只能有一个活跃 session
- 认证方式可配：本地密码 / CAS / OAuth（通过 `.env` 切换）

### 5.2 考试执行安全

| 措施 | 说明 |
|------|------|
| 服务端计时 | 倒计时以服务端时间为准 |
| IP 白名单 | 考场 IP 段限制 |
| 题目乱序 | 每人题目和选项顺序不同 |
| 防切屏 | Phase 1: 检测+记录+报告监考员。Phase 2: Electron 系统级锁屏 |
| 禁复制粘贴 | 前端禁用右键/选择/复制 |
| 答案实时同步 | 每次作答立即提交服务端 |
| 排队分批 | 防止开考洪峰 |
| 试卷不可逆 | 交卷后不可修改（可配置是否允许重考） |

### 5.3 数据安全

- 密码使用 Bun 内置 `Bun.password.hash`（argon2id）
- 答卷数据在考试期间不可被任何人（包括 Admin）查看
- 所有敏感操作写入 AuditLog
- ID 用 UUID，不暴露自增 ID

### 5.4 网络安全

- CORS 严格限制为 LAN 域名/IP
- Rate limiting：登录严格限制，考试接口适度限制
- 所有 API 输入用 TypeBox 校验
- 安全响应头（X-Content-Type-Options, X-Frame-Options 等）

---

## 六、分阶段交付

### Phase 1（MVP）

核心链路：**出题 → 组卷 → 考试 → 出分**

- [ ] 题库管理（手动 + Excel/CSV 批量导入）
- [ ] 组卷（手动选题 + 随机抽题）
- [ ] 考生管理（自定义字段 + 导入）
- [ ] 多租户（Organization + 数据隔离）
- [ ] 认证（JWT + 角色鉴权）
- [ ] 考试执行（答题界面 + 自动保存 + 计时）
- [ ] 自动批改 + 即时出分
- [ ] 成绩导出（Excel/CSV/PDF）
- [ ] 降级策略（三档自适应）
- [ ] Docker 部署

### Phase 2

- [ ] 考场管理 + IP 限制
- [ ] 监考面板 + WebSocket 实时监控
- [ ] Electron 锁屏客户端
- [ ] 简答题 + AI 辅助批改
- [ ] 外部系统对接（CAS/OAuth）
- [ ] 达标放行 API
- [ ] 可定制导出模板

### Phase 3

- [ ] 题干富内容（LaTeX、化学方程式、代码块）
- [ ] 文件上传题 / 编程题 / 画图题
- [ ] 题库共享与跨租户协作
- [ ] 移动端适配

---

## 七、不做什么（明确排除）

- 不做远程考试 / 公网部署（场景是 LAN）
- 不做视频监考 / 摄像头监控（物理考场有真人监考）
- Phase 1 不做富文本编辑器（出题端纯文本 + 图片）
- Phase 1 不做主观题（简答/论文/画图）
- Phase 1 不做移动端 App
- Phase 1 不做 Electron 锁屏（先保证 Web 端完整可用）
- 不接外部 AI API（AI 批改必须本地部署模型）
