# Release notes archive

Version-specific release notes live in this directory.

- `CHANGELOG.md` at the repository root is the canonical chronological release
  history.
- `docs/releases/vX.Y.Z.md` may contain a longer human-readable summary for a
  specific release.
- The Git tag and GitHub Release for a version must point to the same `master`
  commit that contains the corresponding changelog entry.
