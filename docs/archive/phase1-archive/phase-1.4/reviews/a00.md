# A00 审计报告

**Job**: P1.4-A00 DB Reality Check Spike
**审计日期**: 2026-06-10
**审计状态**: PASS (有条件)

---

## 1. 验收标准逐条检查

| # | 验收标准 | 状态 | 证据 |
|---|---------|------|------|
| 1 | ADR 文档产出，明确选定方案及理由 | **PASS** | `docs/archive/phase-1.4/adr-db-dual-dialect.md` (121行) |
| 2 | 验证 repo 在 SQLite 和 PG 下的 list/getById/create 均通过 | **PASS** | 12/12 tests pass, 覆盖 create/findById/list/update/delete |
| 3 | 方案明确回答：是否统一到 async API | **PASS** | ADR Open Questions 第1行：Yes |
| 4 | 方案明确回答：现有 sync 调用点的迁移策略 | **PASS** | ADR Migration Strategy 表 + Open Questions 第2行 |

---

## 2. Scope 合规性

| Scope 要求 | 状态 | 说明 |
|-----------|------|------|
| 创建实验性 `AnyDatabase` 接口定义 | **N/A** | `types.ts` 已有 `AnyDatabase` 定义，无需新增 |
| 选 1 个简单 repo（如 courseRepo）做概念验证 | **PASS** | spike 用 courseRepo 做 PoC |
| 在 SQLite 和 PG 两种环境下运行测试 | **PASS** | 6 SQLite + 6 PG tests |
| 产出 ADR 文档 | **PASS** | `adr-db-dual-dialect.md` |

---

## 3. Allowed Changes 合规性

| Allowed | 实际变更 | 合规 |
|---------|---------|------|
| `packages/db/src/types.ts` — 可能扩展定义 | 未修改 | **OK** |
| 新建实验性文件 | `packages/db/src/spike/dual-dialect.test.ts` | **OK** |
| 修改 1 个 repo 做验证 | 未修改现有 repo（spike 为独立测试文件） | **OK** |

### 额外变更（A00 范围外但合理）

| 文件 | 变更 | 评估 |
|------|------|------|
| `packages/db/src/postgres.ts` | `migratePostgres()` 改为 `async` | **合理** — 修复了 bug（原函数返回 void 但内部 `migrate()` 返回 Promise） |
| `docker-compose.dev.yml` | 新增 PostgreSQL profile + db service | **属于 A03 范围** — 提前做了 A03 的一部分工作，但为 spike 必要基础设施 |

> **建议**: docker-compose.dev.yml 的变更属于 A03 scope。A00 spike 需要 PG 环境，但不应该正式修改 dev compose。当前变更无害（profile 隔离），正式归入 A03 时需重新审计。

---

## 4. Forbidden Changes 合规性

| Forbidden | 状态 | 证据 |
|-----------|------|------|
| 禁止新增 `as any` / `as unknown as` | **PASS** | spike 代码中 0 处 `as any`，0 处 `as unknown as` |
| 禁止修改 domain / contracts / routes / schema | **PASS** | 未触碰这些目录 |

---

## 5. Required Tests 检查

| 要求 | 状态 | 说明 |
|------|------|------|
| 1 个 repo 的 SQLite 测试通过 | **PASS** | 6 tests, in-memory SQLite |
| 1 个 repo 的 PG 测试通过 | **PASS** | 6 tests, Docker PG 16 |

### 测试覆盖的方法

- `create` — 插入并验证返回字段
- `findById` — 按ID查找，存在/不存在两种场景
- `list` — 租户隔离列表
- `update` — 部分更新
- `delete` — 删除并验证

### 测试质量问题

| # | 问题 | 严重性 | 状态 |
|---|------|--------|------|
| 1 | 测试间共享 DB 状态，`list` 测试依赖前序 `create` 的数据 | Low | 可接受（spike 性质） |
| 2 | PG 测试未做 conditional skip（PG 不可用时应 skip 而非 fail） | Medium | **需修复** |
| 3 | PG teardown 使用 `delete` 而非 `TRUNCATE CASCADE`，外键顺序脆弱 | Low | 可接受（仅 courses 依赖 organizations） |
| 4 | 未使用的 `import { sql } from "drizzle-orm"` | Low | **需修复** |

---

## 6. Required Docs 检查

| 要求 | 状态 | 路径 |
|------|------|------|
| `docs/archive/phase-1.4/adr-db-dual-dialect.md` | **PASS** | 121 行，包含 Context/Decision/Consequences/Open Questions |

### ADR 内容审计

| 章节 | 状态 | 说明 |
|------|------|------|
| Context | **PASS** | 准确描述了现状问题 |
| Decision | **PASS** | 明确选定方案 |
| Key Design Points | **PASS** | 5 个设计要点 |
| Migration Strategy | **PASS** | 给出逐层迁移策略 |
| Consequences (Positive) | **PASS** | |
| Consequences (Negative) | **PASS** | 如实列出 overhead 和 complexity |
| Open Questions | **PASS** | 4 个问题全部回答 |
| Related | **PASS** | |

---

## 7. Non-goals 合规性

| Non-goal | 状态 |
|----------|------|
| 不做全量迁移 | **PASS** — 仅 PoC |
| 不修改业务路由、domain 层、schema 文件 | **PASS** |

---

## 8. 发现的问题及修复

### Issue 1: 未使用的 `sql` import (Low)

**文件**: `packages/db/src/spike/dual-dialect.test.ts:13`
**问题**: `import { sql } from "drizzle-orm"` 未在代码中使用
**修复**: 删除该 import

### Issue 2: PG 测试未做 conditional skip (Medium)

**文件**: `packages/db/src/spike/dual-dialect.test.ts:276-294`
**问题**: PG 测试在 PG 不可用时会 fail 而非 skip，CI 环境可能无 PG
**修复**: 在 PG 测试前检测连接可用性，不可用时 skip

### Issue 3: Background 描述中的 `as any` 计数偏差 (Low)

**文件**: `docs/archive/phase-1.4/02-architecture-jobs.md:15`
**问题**: Background 写 "3 处 `as any`"，实际 `attemptRepo.ts` 有 4 处 `as any`
**修复**: 修正为 4 处

---

## 9. 审计结论

**状态**: **PASS**

A00 spike 达成了全部验收标准。核心结论（async + isSqlite 分支 + 类型收窄）经过验证且文档完整。

3 个发现的问题为 Low-Medium 级别，不影响 spike 结论的有效性，但应在合并前修复。
