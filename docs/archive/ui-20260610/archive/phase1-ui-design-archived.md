# Phase 1 UI Design Archived

> This document is archived as historical UI planning material.
> It is not the current single source of truth.
> Current implementation should follow:
>
> - `docs/ui/README.md`
> - `docs/ui/00-ui-principles.md`
> - `docs/ui/02-design-tokens.md`
> - `docs/ui/jobs/*.md`
>
> If this document conflicts with the current tech stack or job files,
> `docs/ui/03-tech-stack.md` and `docs/ui/jobs/*.md` take precedence.

---

# Phase 1 UI Design

> 所有界面遵循 shadcn/ui 设计系统 + TailwindCSS v4。
> 用户界面文字全部中文。wireframe 中的业务数据仅为示例；产品标题、机构名、考试名、考生字段必须来自配置或业务数据。
> 项目结构基于 pnpm workspace monorepo: `apps/web/`, `apps/api/`, `packages/`。

注意：本文档中的 ASCII wireframe 只表达信息结构和布局关系，不代表最终视觉样式。实现时不得复刻线框图的黑色边框、裸表格、裸按钮。最终视觉必须使用 shadcn/ui 组件、Tailwind token、Card/Table/Dialog/Button 等设计系统组件。

---

## 1. Design Tokens

### Color Palette (shadcn/ui neutral base)

```
Primary:     用于主按钮、活跃导航、链接
Destructive: 用于删除、警告、未通过
Muted:       用于背景、禁用状态、次要文字
Accent:      用于 hover、选中态
Border:      用于分割线、边框
Background:  页面背景 / 卡片背景 / 弹窗背景
```

不使用紫色/渐变。主色调跟随 shadcn 默认 neutral 方案，干净专业。

### Typography

```
页面标题:    text-2xl font-semibold
区域标题:    text-lg font-medium
正文:        text-sm
次要文字:    text-sm text-muted-foreground
标签/辅助:   text-xs text-muted-foreground
```

### Spacing

统一使用 Tailwind spacing scale: `p-4`(16px), `gap-4`(16px), `p-6`(24px)。不使用非标准值。

### Accessibility Tokens

```
Focus ring:      ring-2 ring-ring ring-offset-2
Color contrast:  文本 ≥ 4.5:1, 大文本 ≥ 3:1
Touch targets:   交互元素最小 44x44px
Animation:       respect prefers-reduced-motion
```

### Product Generalization Tokens

所有产品级标题和机构级显示信息都不能硬编码。UI 组件应从 `BrandingContext` / `OrganizationSettings` 中读取：

```ts
type BrandingView = {
  productName: string;
  productSubtitle?: string;
  footerText?: string;
  organizationDisplayName?: string;
};
```

初始默认值和 fallback：

| 字段                      | 默认值                                             | 可由管理员修改 |
| ------------------------- | -------------------------------------------------- | -------------- |
| `productName`             | 内网考试平台                                       | 是             |
| `productSubtitle`         | 初始值为“机构内部测评与准入认证”，保存空值时不显示 | 是             |
| `footerText`              | 空                                                 | 是             |
| `organizationDisplayName` | 当前 `Organization.displayName`                    | 是             |

禁止在生产组件的默认文案中写死“校内考试”“校园内网考试平台”“大学”“学生”“学号”“工号”“实验室”“化学”“物理”“数学”等场景词。下方 wireframe 中的业务数据均为示例；文档和 demo fixture 可以使用场景化示例数据，但必须标记为示例。

---

## 2. Navigation Structure

### 2.1 Layout Tree

```
/ (redirect to role-based dashboard)
├── /login                          # 登录页（全屏，无侧栏）
│
├── /admin/*                        # Admin/Teacher 共享后台
│   ├── Layout: Sidebar + Header
│   ├── /admin/dashboard            # 仪表盘
│   ├── /admin/organizations        # 机构管理（SuperAdmin）
│   ├── /admin/users                # 用户管理（Admin/Teacher/Proctor）
│   ├── /admin/candidates           # 考生管理（动态字段表格）
│   ├── /admin/settings             # 平台与机构设置
│   ├── /admin/candidate-fields     # 考生字段配置
│   ├── /admin/courses              # 课程管理
│   ├── /admin/questions            # 题库管理
│   ├── /admin/questions/new        # 新建题目
│   ├── /admin/questions/:id/edit   # 编辑题目
│   ├── /admin/questions/import     # 批量导入题目
│   ├── /admin/exams                # 考试管理
│   ├── /admin/exams/new            # 新建考试（组卷）
│   ├── /admin/exams/:id            # 考试详情（考生列表+状态）
│   ├── /admin/exams/:id/scores     # 成绩管理
│   ├── /admin/attempts/:id         # 答卷详情（Teacher 视角）
│   └── /admin/system               # 系统健康
│
└── /exam/*                         # 考生端（独立布局）
    ├── Layout: Fullscreen, minimal
    ├── /exam/list                  # 可参加的考试列表
    ├── /exam/:id/start             # 开始考试确认页（含排队）
    ├── /exam/:id/take              # 答题界面（全屏）
    └── /exam/:id/result            # 考试结果（双态：即时出分/等待公布）
```

### 2.2 Sidebar Navigation (Admin/Teacher)

```
┌──────────────────────┐
│  🎓 {{productName}}    │
│                       │
│  ── 题库 ──          │
│  课程管理             │
│  题目管理             │
│  题目导入             │
│                       │
│  ── 考试 ──          │
│  考试管理             │
│  成绩查询             │
│                       │
│  ── 管理 ──          │  ← Admin only
│  机构管理             │  ← SuperAdmin only
│  用户管理             │
│  考生管理             │
│  平台设置             │
│  考生字段             │
│  系统状态             │
│                       │
│  ──── 底部 ────      │
│  👤 {{displayName}} | 退出 │
└──────────────────────┘
```

侧栏宽度: `w-56`(224px)，可折叠为 icon-only `w-14`(56px)。

---

### 2.3 Job → Section Reference

> 本文档章节与 `docs/phase1-plan.md` Job 的对应关系。

| Section | Title                 | Job |
| ------- | --------------------- | --- |
| §3.1    | 登录页                | J3  |
| §3.2    | Admin Dashboard       | J9  |
| §3.3    | 题目管理页            | J5A |
| §3.4    | 新建/编辑题目页       | J5A |
| §3.5    | 题目导入页            | J5A |
| §3.6    | 新建考试页（组卷）    | J5B |
| §3.7    | 考生端考试列表        | J6  |
| §3.8    | 考生端答题界面        | J6  |
| §3.9    | 考试结果页            | J7  |
| §3.10   | 开始考试确认页        | J6  |
| §3.11   | 考试详情页（Teacher） | J5B |
| §3.12   | 答卷详情页（Teacher） | J8  |
| §3.13   | 考生字段配置页        | J4  |
| §3.20   | 平台与机构设置页      | J4  |
| §3.14   | 课程管理页            | J5A |
| §3.15   | 系统健康页            | J9  |
| §3.16   | 考生导入弹窗          | J4  |
| §3.17   | 用户管理页            | J4  |
| §3.18   | 考生管理页            | J4  |
| §3.19   | 成绩管理页（Teacher） | J8  |

---

## 3. Page Designs

### 3.1 登录页

```
┌──────────────────────────────────────────────────┐
│                                                    │
│              ┌─────────────────────┐              │
│              │  🎓 {{productName}}  │              │
│              │                      │              │
│              │  用户名              │              │
│              │  ┌─────────────────┐│              │
│              │  │                 ││              │
│              │  └─────────────────┘│              │
│              │                      │              │
│              │  密码                │              │
│              │  ┌─────────────────┐│              │
│              │  │                 ││              │
│              │  └─────────────────┘│              │
│              │                      │              │
│              │  ┌─────────────────┐│              │
│              │  │     登  录       ││              │
│              │  └─────────────────┘│              │
│              │                      │              │
│              └─────────────────────┘              │
│                                                    │
│              {{productSubtitle}}                     │
└──────────────────────────────────────────────────┘
```

- 全屏居中卡片，`max-w-sm`(384px)
- 背景: 纯色（`bg-background`），无渐变
- 登录失败: 表单下方红色提示文字（非 alert 弹窗）
- 表单使用 `label` + `htmlFor` 关联，回车键提交

---

### 3.2 Admin Dashboard

```
┌──────────────────────────────────────────────────────┐
│ Header: 面板首页                    [🔔] [👤 示例用户A]  │
├────────┬─────────────────────────────────────────────┤
│        │                                              │
│ Side   │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│ bar    │  │ 题目  │ │ 考试  │ │ 考生  │ │ 今日  │      │
│        │  │ 总数  │ │ 进行中│ │ 总数  │ │ 考试  │      │
│        │  │  128  │ │   3  │ │  450  │ │   2  │      │
│        │  └──────┘ └──────┘ └──────┘ └──────┘      │
│        │                                              │
│        │  最近考试                                     │
│        │  ┌──────────────────────────────────────┐  │
│        │  │ 安全准入测评  │ 进行中 │ 32/50人    │  │
│        │  │ 设备操作认证  │ 未开始 │ 已报名28人 │  │
│        │  │ 年度合规考试  │ 已结束 │ 平均78分   │  │
│        │  └──────────────────────────────────────┘  │
│        │                                              │
└────────┴─────────────────────────────────────────────┘
```

- 4 个统计卡片用 `Card` 组件，一行排列
- 统计数字 `text-3xl font-bold`
- 最近考试用 `Table` 组件
- 数据加载时显示 `Skeleton` 骨架屏

---

### 3.3 题目管理页

```
┌─────────────────────────────────────────────────────┐
│ 题目管理                              [+ 新建题目]   │
│                                        [导入题目]    │
│                                                      │
│ 筛选:                                                │
│ [课程 ▼] [题型 ▼] [难度 ▼] [标签...]  [搜索...]     │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ ☐ │ 单选 │ 关于高温设备的安全操作... │ 安全 │ 5分  │ │
│ │ ☐ │ 多选 │ 以下哪些是正确的实验... │ 安全 │ 3分  │ │
│ │ ☐ │ 填空 │ 作业现场安全设备应放置在____│ 安全 │ 5分  │ │
│ │ ☐ │ 判断 │ 作业现场内可以饮食()     │ 安全 │ 2分  │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 显示 1-20 / 共 128 条                 [< 1 2 3 ... >]│
└─────────────────────────────────────────────────────┘
```

- 顶部操作栏: 按钮 + 筛选器
- 表格列: 选择框、题型(badge)、题干(截断显示)、标签(小chip)、分值
- 操作列: 编辑、删除（icon 按钮，带 `aria-label`）
- 筛选器用 `Select` + `Input` 组件

---

### 3.4 新建/编辑题目页

**单选题 / 多选题**：

```
┌─────────────────────────────────────────────────────┐
│ 新建题目                              [保存] [取消]  │
│                                                      │
│ 课程     [安全准入培训 ▼]                          │
│ 题型     [单选题 ▼]                                  │
│ 分值     [5]     难度 [●●●○○]                       │
│ 标签     [安全] [设备] [+ 添加]                      │
│                                                      │
│ 题干                                                  │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 关于高温设备的操作，以下哪项是正确的？              │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 选项（点击 ○/● 标记正确答案）                         │
│ ┌──────────────────────────────────────────────────┐ │
│ │ ○ A   错误操作 A              [🗑]     │ │
│ │ ● B   正确操作 B    [🗑]     │ │
│ │ ○ C   错误操作 C              [🗑]     │ │
│ │ ○ D   错误操作 D              [🗑]     │ │
│ └──────────────────────────────────────────────────┘ │
│ [+ 添加选项]                                         │
│                                                      │
│ ── 预览 ──────────────────────────────────────────── │
│ 关于高温设备的操作，以下哪项是正确的？                  │
│ ○ A. 错误操作 A                           │
│ ● B. 正确操作 B                 │
│ ○ C. 错误操作 C                           │
│ ○ D. 错误操作 D                           │
└─────────────────────────────────────────────────────┘
```

**填空题**：

```
┌─────────────────────────────────────────────────────┐
│ 新建题目                              [保存] [取消]  │
│                                                      │
│ 课程     [安全准入培训 ▼]                          │
│ 题型     [填空题 ▼]                                  │
│ 分值     [5]     难度 [●●○○○]                       │
│                                                      │
│ 题干（用 ____ 标记填空位置）                          │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 作业现场安全设备应放置在____，且____附近不得堆放杂物。│ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 标准答案（每个空一行，支持多个可接受答案用 | 分隔）   │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 第1空: 通道处 | 入口 | 出入口                   │ │
│ │ 第2空: 安全设备 | 安全器材                          │ │
│ └──────────────────────────────────────────────────┘ │
│ 匹配模式  [● 精确匹配  ○ 关键词包含]                 │
│                                                      │
│ ── 预览 ──────────────────────────────────────────── │
│ 作业现场安全设备应放置在____，且____附近不得堆放杂物。   │
│ 第1空: [           ]                                │
│ 第2空: [           ]                                │
└─────────────────────────────────────────────────────┘
```

**判断题**：

```
┌─────────────────────────────────────────────────────┐
│ 新建题目                              [保存] [取消]  │
│                                                      │
│ 课程     [安全准入培训 ▼]                          │
│ 题型     [判断题 ▼]                                  │
│ 分值     [2]     难度 [●○○○○]                       │
│                                                      │
│ 题干                                                  │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 作业现场内可以饮食。                                │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 正确答案  [● 错误  ○ 正确]                           │
│                                                      │
│ ── 预览 ──────────────────────────────────────────── │
│ 作业现场内可以饮食。                                   │
│ ○ 正确   ● 错误                                     │
└─────────────────────────────────────────────────────┘
```

- 正确答案直接在选项行内标记（选择题点击 ○/●，判断题二选一，填空题独立输入区）
- 无底部重复的标准答案字段
- 题型切换时整个"选项/答案区"动态替换
- 底部实时预览区模拟考生视角
- `standardAnswer` 必填校验：没有标准答案的题不能用于自动批改

---

### 3.5 题目导入页

```
┌─────────────────────────────────────────────────────┐
│ 批量导入题目                                          │
│                                                      │
│ Step 1: 上传文件                                      │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │                                                    │ │
│ │       📄 拖拽 Excel/CSV 文件到此处               │ │
│ │          或 [点击选择文件]                         │ │
│ │                                                    │ │
│ │       支持 .xlsx .csv 格式                        │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ [下载导入模板]                                        │
│                                                      │
│ Step 2: 预览确认                                      │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ # │ 状态 │ 题型 │ 题干(摘要)    │ 异常           │ │
│ │ 1 │  ✅  │ 单选 │ 关于高温设备... │                │ │
│ │ 2 │  ✅  │ 填空 │ 作业现场灭火... │                │ │
│ │ 3 │  ⚠️  │ 单选 │ 安全操作...   │ 缺少标准答案   │ │
│ │ 4 │  ❌  │      │ sdfgsdfg...  │ 题型无法识别   │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 共识别 25 题  ✅ 有效 23  ⚠️ 警告 1  ❌ 错误 1       │
│                                                      │
│ [导入有效题目 (23题)]  [取消]                         │
└─────────────────────────────────────────────────────┘
```

- Step 分步布局（`Steps` 组件）
- 上传区用拖拽组件
- 预览表格标记异常行，底部汇总
- 警告行可选导入，错误行不可导入

---

### 3.6 新建考试页（组卷）

> Phase 1 仅支持手动选题。随机抽题模式见 wireframe 但标记为 [Phase 2]。

```
┌─────────────────────────────────────────────────────┐
│ 新建考试                                              │
│                                                      │
│ ── 基本信息 ─────────────────────────────────────── │
│ 考试名称  [安全准入测评]                        │
│ 课程      [安全准入培训 ▼]                          │
│ 描述      [本测验用于作业现场准入考核...]                │
│                                                      │
│ ── 考试设置 ─────────────────────────────────────── │
│ 计时方式  [窗口限时 ▼]     ← Phase 1 仅此一种        │
│ 考试时长  [60] 分钟                                   │
│ 开放时间  [2025-03-01] ~ [2025-03-07]                │
│ 及格线    [60] 分  总分 [100] 分                     │
│                                                      │
│ ── 管控设置 ─────────────────────────────────────── │
│ 模式预设  [○ 开卷  ● 闭卷]                           │
│                                                      │
│ ☑ 题目乱序    ☑ 选项乱序    ☐ 检测切屏              │
│ ☐ 禁止复制    ☐ 排队进入    ☐ IP 限制               │
│ ☑ 允许回看    ☐ 即时出分    ☐ 强制锁屏              │
│                                                      │
│ 重考策略  [最多3次 ▼]    冷却 [60] 分钟              │
│ 取分策略  [取最高分 ▼]    ☐ 通过后不可重考           │
│                                                      │
│ ── 选择题目 ─────────────────────────────────────── │
│ 选题方式  [● 手动选题]              ← Phase 1 默认   │
│                                                      │
│ 已选题目 (15题 / 100分)                               │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 单选 │ 关于高温设备的操作... │ 5分  │ [移除]      │ │
│ │ 单选 │ 以下安全设备类型...   │ 5分  │ [移除]      │ │
│ │ ...                                              │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ [+ 从题库添加]                                       │
│                                                      │
│ ── 预览与发布 ───────────────────────────────────── │
│ [预览考试]  [保存草稿]  [发布考试]                    │
└─────────────────────────────────────────────────────┘
```

**随机抽题模式** [Phase 2]：

```
│ ── 选择题目 ─────────────────────────────────────── │
│ 选题方式  [○ 手动选题  ● 随机抽题]                   │
│                                                      │
│ 题库来源  [安全准入培训 ▼]                          │
│ 总题数    [20]    每题分值 [5]  总分: 100             │
│                                                      │
│ 抽题规则                                              │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 题型     │ 抽取数量 │ 难度范围      │ 标签       │ │
│ │ 单选题   │ [10]     │ [1-5]         │ [全部] ▼   │ │
│ │ 多选题   │ [5]      │ [2-5]         │ [安全] ▼   │ │
│ │ 填空题   │ [3]      │ [1-3]         │ [全部] ▼   │ │
│ │ 判断题   │ [2]      │ [1-2]         │ [全部] ▼   │ │
│ │                                            [+ 行] │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ ☑ 题库不足时允许减少题目（不报错）                    │
│ 匹配题库: 128 题可用  预计可抽: 20 题 ✅              │
│                                                      │
│ ── 预览与发布 ───────────────────────────────────── │
│ [预览考试]  [保存草稿]  [发布考试]                    │
└─────────────────────────────────────────────────────┘
```

- 长表单，用分割线分组（Section Header）
- 管控设置: 模式预设按钮切换后自动填充复选框，但可单独改
- 选题区: Phase 1 仅手动模式；随机抽题 UI 预留但 disabled
- 底部固定操作栏

---

### 3.7 考生端 - 考试列表

```
┌─────────────────────────────────────────────────────┐
│  🎓 考试系统                            [👤 示例用户A]   │
├─────────────────────────────────────────────────────┤
│                                                      │
│  可参加的考试                                         │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 安全准入测评                              │ │
│  │ 窗口限时 · 60分钟 · 及格60分                    │ │
│  │ 开放: 2025-03-01 ~ 2025-03-07                  │ │
│  │ 已考 0/3 次    最高分: --                       │ │
│  │                                    [开始考试]   │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 设备操作认证                                │ │
│  │ 窗口限时 · 90分钟 · 及格70分                    │ │
│  │ 开放: 2025-03-10 09:00 ~ 11:00                 │ │
│  │ 已考 1/1 次    最高分: 82 ✅                    │ │
│  │                                    [查看结果]   │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ── 已结束 ──────────────────────────────────────── │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │ 年度合规考试（已结束）                           │ │
│  │ 得分: 78/100  ✅ 通过                           │ │
│  │                                    [查看结果]   │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 考试用卡片展示，区分"可参加"和"已结束"
- 每张卡片: 考试名称、计时方式/时长/及格线、考试次数/最高分、操作按钮
- 已通过考试显示 ✅ 和分数
- Phase 1 计时方式仅显示"窗口限时"

---

### 3.8 考生端 - 答题界面（核心页面）

```
┌─────────────────────────────────────────────────────┐
│ 安全准入测评  │  ⏱ 剩余 58:32  │  ● 已答 3/15 │ 交卷│
├────────┬────────────────────────────────────────────┤
│ 题号   │  4. 关于高温设备的操作，以下哪项正确？       │
│        │                                             │
│ ① ●   │  ┌ 图片 ──────────────────────────────┐   │
│ ② ●   │  │  [实验操作示意图]                    │   │
│ ③ ●   │  └─────────────────────────────────────┘   │
│ ④ ←   │                                             │
│ ⑤ ○   │  ○  A. 错误操作 A                │
│ ⑥ ○   │                                             │
│ ⑦ ◉   │  ○  B. 将高温设备缓慢倒入水中                │
│ ⑧ ○   │                                             │
│  ...   │  ●  C. 正确操作 C        │
│        │                                             │
│        │  ○  D. 错误操作 D                       │
│        │                                             │
│ ● 已答 │  ◀ 上一题    ⚑ 标记    下一题 ▶            │
│ ○ 未答 │         保存中... → ✓ 已保存                │
│ ◉ 标记 │                                             │
├────────┴────────────────────────────────────────────┤
│  ● 已答: 3  │  ○ 未答: 12  │  ◉ 标记: 1  │ 共 15 题 │
└─────────────────────────────────────────────────────┘
```

**全屏模式**：无侧栏、无导航。顶部紧凑工具栏，最大化答题空间。

- **左侧题号栏** (`w-20`, 80px): 固定，颜色标记状态，当前题高亮边框，50+ 题时两列排列，超出滚动
- **顶部工具栏**: 考试名称、倒计时(红字<5min)、进度、交卷按钮
- **右侧答题区**: 题目 + 选项/输入框
- **底部导航**: 上/下题、标记按钮
- **底部状态栏**: 答题统计
- **自动保存状态**: 底部导航旁显示 `保存中...` → `✓ 已保存` / `⚠ 保存失败`（Answer Save Protocol）

**交卷确认弹窗**:

```
┌──────────────────────────────┐
│  确认交卷？                   │
│                               │
│  ⚠ 未答题: 12                │
│  ⚑ 已标记: 1                 │
│                               │
│  交卷后不可修改。              │
│                               │
│  [继续答题]    [确认交卷]     │
└──────────────────────────────┘
```

交卷时通过 `submitAttempt(ctx, attemptId)` command 提交，服务端判定是否超时。

---

### 3.9 考试结果页

**变体 A: 即时出分 (`showResultImmediately=true`)**

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│              安全准入测评                        │
│                                                      │
│              ┌─────────────┐                         │
│              │    92 分     │                         │
│              │   ✅ 通过    │                         │
│              │  及格线: 60  │                         │
│              └─────────────┘                         │
│                                                      │
│  答题详情（15题）                                     │
│  ┌──────────────────────────────────────────────────┐│
│  │ # │ 题型 │ 你的答案    │ 正确答案 │ 得分        ││
│  │ 1 │ 单选 │ C          │ C       │ 5/5  ✅     ││
│  │ 2 │ 单选 │ B          │ B       │ 5/5  ✅     ││
│  │ 3 │ 填空 │ 合格证...  │ 合格证  │ 5/5  ✅     ││
│  │ 4 │ 单选 │ A          │ C       │ 0/5  ❌     ││
│  │ ...                                              ││
│  └──────────────────────────────────────────────────┘│
│                                                      │
│              [返回考试列表]                            │
│                                                      │
└─────────────────────────────────────────────────────┘
```

**变体 B: 等待公布 (`showResultImmediately=false`)**

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│              安全准入测评                        │
│                                                      │
│              ┌─────────────┐                         │
│              │  已交卷 ✅   │                         │
│              │              │                         │
│              │  等待成绩公布 │                         │
│              └─────────────┘                         │
│                                                      │
│              [返回考试列表]                            │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 变体 A: 答对 ✅ 绿色，答错 ❌ 红色。颜色 + 图标双重标识（非仅靠颜色）
- 变体 B: 只确认已交卷，不显示任何答案或分数
- 填空题答案过长时截断，hover 或点击展开显示完整内容

---

### 3.10 开始考试确认页

```
┌─────────────────────────────────────────────────────┐
│  🎓 考试系统                            [👤 示例用户A]   │
├─────────────────────────────────────────────────────┤
│                                                      │
│              安全准入测评                        │
│                                                      │
│  ┌──────────────────────────────────────────────────┐│
│  │ 计时方式    窗口限时                              ││
│  │ 考试时长    60 分钟                               ││
│  │ 及格分数    60 分                                 ││
│  │ 题目数量    15 题                                 ││
│  │ 管控措施    题目乱序、选项乱序、检测切屏           ││
│  │ 已考次数    0 / 3 次                              ││
│  └──────────────────────────────────────────────────┘│
│                                                      │
│  ⚠ 开始后倒计时立即启动，中途不可暂停。              │
│  ⚠ 考试期间请保持网络连接。                          │
│                                                      │
│         [返回列表]    [确认开始考试]                  │
│                                                      │
└─────────────────────────────────────────────────────┘
```

**排队状态（闭卷统考 + requireQueue）**：

```
│                                                      │
│  ⏳ 考试即将开始                                     │
│                                                      │
│  前面还有 12 人排队中                                 │
│ 预计等待: 约 30 秒                                    │
│                                                      │
│  ████████░░░░░░  进度                                 │
│                                                      │
│  请勿关闭此页面                                       │
│                                                      │
```

- 确认页显示考试配置摘要，让考生了解规则
- 排队页实时更新等待人数和预计时间
- 轮到时自动跳转到答题界面
- 开始考试调用 `startAttempt(ctx, examId, candidateId)` command

---

### 3.11 考试详情页（Teacher 视角）

```
┌─────────────────────────────────────────────────────┐
│ 安全准入测评 ← 考试管理                               │
│                                                      │
│ ┌─ 基本信息 ──────────────────────────────────────┐ │
│ │ 状态: 已发布  │  窗口限时 60min  │  及格 60分    │ │
│ │ 开放: 2025-03-01 ~ 2025-03-07                   │ │
│ │ 已参加: 32/50 人  已通过: 28 人  通过率: 87.5%  │ │
│ └─────────────────────────────────────────────────┘ │
│                                                      │
│ [编辑考试]  [结束考试]  [导出成绩]                     │
│                                                      │
│ ── 考生列表 ──────────────────────────────────────  │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 编号  │ 姓名 │ 状态   │ 得分 │ 通过 │ 操作      │ │
│ │ E001  │ 示例用户A │ 已交卷 │ 92  │ ✅  │ 查看答卷 │ │
│ │ E002  │ 示例用户B │ 考试中 │ --  │ --  │ --       │ │
│ │ E003  │ 示例用户C │ 未开始 │ --  │ --  │ --       │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 顶部考试配置摘要 + 实时统计
- 考生列表：显示每人状态和成绩，可跳转 attempt 详情页
- "考生列表"列头按机构 CandidateField 动态生成（上例中是编号）

---

### 3.12 答卷详情页（Teacher 视角）

```
┌─────────────────────────────────────────────────────┐
│ 示例用户A - 安全准入测评 ← 成绩管理                        │
│                                                      │
│ 得分: 92/100  ✅ 通过  │  提交: 2025-03-05 10:32    │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ # │ 题型 │ 考生答案         │ 正确答案    │ 得分 │ │
│ │ 1 │ 单选 │ C               │ C          │ 5/5  │ │
│ │ 2 │ 多选 │ A,B             │ A,B,D      │ 2/5  │ │
│ │ 3 │ 填空 │ 合格证          │ 合格证     │ 5/5  │ │
│ │ 4 │ 单选 │ A               │ C          │ 0/5  │ │
│ │ ...                                             │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ [返回成绩列表]                                        │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 完整展示考生每道题的作答和批改结果
- 多选题部分正确时显示扣分逻辑（半分）
- Phase 1 不做 PDF 导出按钮（Phase 2）
- 路由: `/admin/attempts/:id`（不是 papers/:id）

---

### 3.13 考生字段配置页

```
┌─────────────────────────────────────────────────────┐
│ 考生字段配置                              [+ 添加字段]│
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 字段名称 │ 显示名 │ 类型   │ 必填 │ 唯一标识 │ 操作│ │
│ │ identifier│ 编号  │ 文本   │  ✅  │  ✅     │ [↕][✏][🗑]│
│ │ name     │ 姓名  │ 文本   │  ✅  │  ☐     │ [↕][✏][🗑]│
│ │ group    │ 分组  │ 下拉   │  ☐  │  ☐     │ [↕][✏][🗑]│
│ │ contact  │ 联系方式  │ 文本   │  ☐  │  ☐     │ [↕][✏][🗑]│
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ ⚠ 唯一标识字段只能有一个，用于考生去重和导入匹配。    │
│ ⚠ 修改字段会影响已有的导入模板格式。                  │
│                                                      │
│ [预览导入模板]  [下载模板]                             │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 表格可拖拽排序（↕ 按钮）
- 唯一标识列只能勾选一个（radio 行为）
- 类型下拉：文本 / 数字 / 下拉选择
- 下拉类型需额外配置选项列表
- 预览模板：根据当前字段配置生成示例 Excel 列头

---

### 3.14 课程管理页

```
┌─────────────────────────────────────────────────────┐
│ 课程管理                                [+ 新建课程] │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 课程名称          │ 代码    │ 题目数 │ 操作      │ │
│ │ 安全准入培训    │ SAFE01 │ 128   │ [✏][🗑]  │ │
│ │ 设备操作培训      │ OPS01 │  56   │ [✏][🗑]  │ │
│ │ 安全培训          │ SAFE01 │  89   │ [✏][🗑]  │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 简单 CRUD 表格
- "题目数"是关联统计（该课程下的题目数量）

---

### 3.15 系统健康页 [Phase 2 完整版，Phase 1 仅基本健康检查]

Phase 1 基本版本：

```
┌─────────────────────────────────────────────────────┐
│ 系统状态                                              │
│                                                      │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│ │ CPU 使用  │ │ 内存使用  │ │ DB 响应   │              │
│ │   23%    │ │  45%     │ │  12ms    │              │
│ └──────────┘ └──────────┘ └──────────┘              │
│                                                      │
└─────────────────────────────────────────────────────┘
```

Phase 2 完整版本（降级配置 + 事件流 + 手动切换）：

```
┌─────────────────────────────────────────────────────┐
│ 系统状态                                              │
│                                                      │
│ 当前档位:  🟢 正常         [手动切换 ▼]              │
│                                                      │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐│
│ │ CPU 使用  │ │ 内存使用  │ │ DB 响应   │ │ 活跃连接  ││
│ │   23%    │ │  45%     │ │  12ms    │ │   28     ││
│ │ ██████░░ │ │ ████████░│ │  正常     │ │  正常    ││
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘│
│                                                      │
│ ── 降级配置 ─────────────────────────────────────── │
│ 省电阈值: CPU [70]%  内存 [80]%  DB [500]ms         │
│ 极限阈值: CPU [90]%  内存 [95]%  DB [2000]ms        │
│                                                      │
│ ── 最近事件 ──────────────────────────────────────  │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 10:32 │ 自动切换为正常模式（CPU 恢复至 23%）       │ │
│ │ 09:15 │ 自动切换为省电模式（CPU 达到 72%）         │ │
│ │ 09:00 │ 考试"安全准入测评"开始，42 人同时在线      │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- Phase 1: 仅显示 3 个基本指标卡片
- Phase 2: 4 个实时指标卡片每 10s 刷新，档位指示，降级配置，事件流

---

### 3.16 考生导入弹窗

```
┌─────────────────────────────────────────────────────┐
│ 批量导入考生                                     [✕] │
│                                                      │
│ Step 1: 上传文件                                      │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │       📄 拖拽 Excel/CSV 文件到此处               │ │
│ │          或 [点击选择文件]                         │ │
│ │       支持 .xlsx .csv 格式                        │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 字段说明: 编号(必填,唯一) | 姓名(必填) | 分组 | 联系方式 │
│ [下载导入模板]                                        │
│                                                      │
│ Step 2: 预览确认                                      │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ # │ 状态 │ 编号  │ 姓名 │ 分组   │ 异常         │ │
│ │ 1 │  ✅  │ E001 │ 示例用户A │ 一组 │               │ │
│ │ 2 │  ✅  │ E002 │ 示例用户B │ 二组 │               │ │
│ │ 3 │  ⚠️  │ E001 │ 示例用户A │ 一组 │ 编号已存在(更新)│ │
│ │ 4 │  ❌  │      │ 示例用户C │ 一组 │ 编号缺失      │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 共 23 人  ✅ 新增 18  ⚠️ 更新 3  ❌ 错误 2          │
│                                                      │
│ [确认导入]  [取消]                                    │
└─────────────────────────────────────────────────────┘
```

- 类似题目导入的 Step 流程
- 字段说明行根据当前机构的 CandidateField 动态生成
- 唯一标识重复时标记为"更新"（非错误），自动更新已有信息
- 必填字段缺失标记为错误

---

### 3.19 成绩管理页（Teacher）

```
┌─────────────────────────────────────────────────────┐
│ 安全准入测评 - 成绩管理                               │
│                                                      │
│ [导出 CSV]                                           │
│                                                      │
│ 筛选: [全部 ▼] [通过/未通过 ▼]  [搜索姓名/编号...] │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 编号   │ 姓名 │ 分组     │ 得分 │ 通过 │ 操作   │ │
│ │ E0001  │ 示例用户A │ 一组   │ 92  │ ✅  │ 详情   │ │
│ │ E0002  │ 示例用户B │ 二组   │ 78  │ ✅  │ 详情   │ │
│ │ E0003  │ 示例用户C │ 一组   │ 45  │ ❌  │ 详情   │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 统计: 平均 72.3  最高 98  最低 32  通过率 78%         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 顶部操作: Phase 1 仅 CSV 导出（Excel / PDF / Word → Phase 2）
- 表格列头动态（按机构 CandidateField 生成）
- 底部统计行
- 点击"详情"跳转 attempt 详情页 `/admin/attempts/:id`

---

### 3.17 用户管理页（Admin/Teacher/Proctor）

```
┌─────────────────────────────────────────────────────┐
│ 用户管理                            [+ 添加用户]     │
│                                                      │
│ 筛选: [全部角色 ▼]  [搜索姓名/用户名...]              │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 用户名  │ 姓名   │ 角色   │ 状态  │ 操作        │ │
│ │ admin   │ 管理员 │ Admin │ 正常  │ [编辑][禁用]│ │
│ │ teacher1│ 管理员A │ 教师  │ 正常  │ [编辑][禁用]│ │
│ │ proctor1│ 监考员A │ 监考  │ 正常  │ [编辑][禁用]│ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 管理非考生角色（Admin / Teacher / Proctor）
- 考生在独立的"考生管理"页管理

---

### 3.18 考生管理页

```
┌─────────────────────────────────────────────────────┐
│ 考生管理                      [+ 添加考生] [导入]     │
│                                                      │
│ [导出名单]                                           │
│                                                      │
│ 筛选: [搜索姓名/编号...]                              │
│                                                      │
│ ┌──────────────────────────────────────────────────┐ │
│ │ 编号  │ 姓名 │ 分组   │ 考试次数 │ 操作          │ │
│ │ E001  │ 示例用户A │ 一组 │ 3       │ [编辑][禁用]  │ │
│ │ E002  │ 示例用户B │ 二组 │ 1       │ [编辑][禁用]  │ │
│ └──────────────────────────────────────────────────┘ │
│                                                      │
│ 表头列根据机构 CandidateField 动态生成                │
│                                                      │
└─────────────────────────────────────────────────────┘
```

- 专管考生（role=Candidate）
- 表头列按机构的 CandidateField 动态生成（上例中机构定义了编号、姓名、分组）
- 导入按钮弹出 §3.16 的导入向导

---

### 3.20 平台与机构设置页

> J4 实现。此页解决产品泛化问题：同一套系统可以部署为校内考试、培训测评、岗位准入、设备操作认证、协会考试等，不由代码写死。

```
┌─────────────────────────────────────────────────────┐
│ 平台与机构设置                          [保存设置]   │
│                                                      │
│ ── 展示信息 ─────────────────────────────────────── │
│ 产品标题      [内网考试平台]                         │
│ 产品副标题    [机构内部测评与准入认证]                │
│ 页脚说明      [由本机构内部部署和维护]                │
│ 机构显示名    [示例机构]                              │
│                                                      │
│ ── 区域设置 ─────────────────────────────────────── │
│ 默认时区      [Asia/Shanghai ▼]                      │
│                                                      │
│ ── 考生身份模板预览 ─────────────────────────────── │
│ 当前唯一标识  编号                                    │
│ 导入列头      编号 | 姓名 | 分组 | 联系方式           │
│                                                      │
│ ── 登录页预览 ───────────────────────────────────── │
│  🎓 内网考试平台                                     │
│  机构内部测评与准入认证                               │
│                                                      │
│ [保存设置] [恢复默认]                                 │
└─────────────────────────────────────────────────────┘
```

- 只有 Admin 可以编辑当前机构设置。
- `productName` 为空时 fallback 为“内网考试平台”。
- `productSubtitle` 和 `footerText` 可为空，前端不要渲染空占位。
- `organizationDisplayName` 为空时 fallback 为当前 `Organization.displayName`。
- 时区影响考试开始时间和截止时间的展示。
- 保存后登录页、后台侧栏、考生端页头立即使用新配置。
- 此页不负责改角色权限，不负责多语言，不负责上传 logo；logo 可放到 Phase 2。

---

## 3.21 QuestionNavItem State Model

答题界面左侧题号导航（§3.8）的每道题有三种视觉状态：

| State      | Symbol | Color  | CSS Token               | Meaning           |
| ---------- | ------ | ------ | ----------------------- | ----------------- |
| unanswered | ○      | gray   | `text-muted-foreground` | not yet answered  |
| answered   | ●      | green  | `text-green-600`        | answer saved      |
| flagged    | ◉      | yellow | `text-yellow-500`       | marked for review |

Additional visual rules:

- Current question: distinct ring highlight (`ring-2 ring-primary`) + slightly elevated background
- Hover on non-current: subtle background change (`bg-accent`)
- 50+ questions: two-column layout within the nav panel with `overflow-y-auto`
- State transitions are client-side and derived from: (1) answer presence in local state, (2) flag toggle state
- SaveIndicator status (saving/saved/failed) does not affect the question nav state — only answer presence matters

---

## 4. Component Inventory

### 4.1 shadcn/ui 需要安装的组件

Phase 1 必需:

```
button, input, label, select, textarea, checkbox, radio-group,
card, table, dialog, dropdown-menu, badge, tabs, separator,
toast, sonner, avatar, skeleton, alert, sheet,
form (react-hook-form + zod), pagination, tooltip, alert-dialog, switch
```

### 4.2 自定义业务组件

> 路径基于 `apps/web/src/`。

| 组件                   | 位置                                           | 说明                                 |
| ---------------------- | ---------------------------------------------- | ------------------------------------ |
| `BrandProvider`        | `components/layout/BrandProvider.tsx`          | 加载并提供平台/机构展示配置          |
| `BrandHeader`          | `components/layout/BrandHeader.tsx`            | 登录页、侧栏、考生端页头统一品牌展示 |
| `AppSidebar`           | `components/layout/AppSidebar.tsx`             | 后台侧栏导航                         |
| `AdminLayout`          | `components/layout/AdminLayout.tsx`            | 后台布局（sidebar + header + main）  |
| `ExamLayout`           | `components/layout/ExamLayout.tsx`             | 考生端布局（minimal header）         |
| `QuestionForm`         | `components/question/QuestionForm.tsx`         | 题目创建/编辑表单                    |
| `QuestionPreview`      | `components/question/QuestionPreview.tsx`      | 题目预览（考生视角）                 |
| `QuestionNav`          | `components/exam/QuestionNav.tsx`              | 答题页左侧题号导航                   |
| `QuestionRenderer`     | `components/exam/QuestionRenderer.tsx`         | 根据题型渲染答题组件                 |
| `SingleChoiceInput`    | `components/exam/SingleChoiceInput.tsx`        | 单选答题组件                         |
| `MultipleChoiceInput`  | `components/exam/MultipleChoiceInput.tsx`      | 多选答题组件                         |
| `FillBlankInput`       | `components/exam/FillBlankInput.tsx`           | 填空答题组件                         |
| `TrueFalseInput`       | `components/exam/TrueFalseInput.tsx`           | 判断答题组件                         |
| `ExamTimer`            | `components/exam/ExamTimer.tsx`                | 倒计时显示（服务端时间）             |
| `ExamConfigForm`       | `components/exam/ExamConfigForm.tsx`           | 考试配置表单（组卷页）               |
| `PlatformSettingsForm` | `components/settings/PlatformSettingsForm.tsx` | 平台与机构展示配置表单               |
| `ImportWizard`         | `components/shared/ImportWizard.tsx`           | 通用导入向导（上传→预览→确认）       |
| `FileUpload`           | `components/shared/FileUpload.tsx`             | 拖拽上传组件                         |
| `StatsCard`            | `components/shared/StatsCard.tsx`              | 仪表盘统计卡片                       |
| `ConfirmDialog`        | `components/shared/ConfirmDialog.tsx`          | 确认弹窗（基于 AlertDialog）         |
| `ConnectionIndicator`  | `components/shared/ConnectionIndicator.tsx`    | 连接状态指示器（绿/黄/红）           |
| `DegradationIndicator` | `components/shared/DegradationIndicator.tsx`   | 系统档位指示器 [Phase 2]             |
| `EmptyState`           | `components/shared/EmptyState.tsx`             | 空状态占位                           |
| `PageHeader`           | `components/shared/PageHeader.tsx`             | 页面标题 + 操作按钮                  |
| `SaveIndicator`        | `components/exam/SaveIndicator.tsx`            | 答案保存状态（保存中/已保存/失败）   |

---

## 5. Interaction Patterns

### 5.1 Answer Save Protocol（答案自动保存）

答题时，每次选择/输入变更后:

1. 本地状态即时更新（客户端内存）
2. 去抖 1-2s 后按 Answer Save Protocol 发送:
   ```
   POST /attempts/:attemptId/answers/:questionId
   { answer, clientSeq, clientSavedAt, baseVersion }
   ```
3. 输入框旁显示状态:
   - 灰色 `保存中...` → 绿色 `✓ 已保存`
   - 保存失败: 红色 `⚠ 保存失败，正在重试...`
4. 服务端返回 `serverVersion`，客户端据此更新 baseVersion
5. 冲突时（`accepted: false`）: 显示冲突提示，以服务端为准

同步写入 localStorage/IndexedDB 作为离线兜底。

### 5.2 倒计时

- 服务端返回 `startedAt` + `durationMinutes`，客户端计算剩余时间
- 每 30s 向服务端同步一次（校正漂移）
- `< 5min` 时倒计时文字变红
- `= 0` 时调用 `submitAttempt()` 自动交卷，弹窗提示"考试时间已到"
- 超时判定以服务端 `deadlineAt` 为准

### 5.3 表单验证

- 使用 react-hook-form + zod
- 实时校验 + 提交时校验
- 错误提示在字段下方，红色文字
- 错误提示用 `role="alert"` + `aria-live="polite"`

### 5.4 Toast 通知

- 成功: 绿色 toast，3s 自动消失
- 错误: 红色 toast，需手动关闭
- 警告: 黄色 toast，5s 自动消失
- 使用 sonner 组件

---

## 6. Empty / Error / Loading States

每个列表页都必须处理三种状态:

```tsx
// Loading: 骨架屏
<Skeleton className="h-10 w-full" />

// Empty: 提示 + 引导操作
<EmptyState
  icon={<QuestionsIcon />}
  title="暂无题目"
  description="点击上方按钮创建第一道题目"
  action={<Button>新建题目</Button>}
/>

// Error: 错误提示 + 重试
<ErrorState message="加载失败" onRetry={refetch} />
```

每个状态组件都设置正确的 ARIA 属性:

- Loading: `aria-busy="true"` + `aria-label="加载中"`
- Empty: `role="status"`
- Error: `role="alert"`

---

## 7. Responsive Design

系统主要在桌面端使用（LAN 考试场景），但仍需基本响应式支持:

- **最小宽度**: 1024px（考试答题界面）
- **Admin 后台**: 响应式表格 + 侧栏折叠
- **考试列表**: 卡片布局，移动端单列
- **答题界面**: 固定布局，不适配移动端（Phase 1 不做移动端）

---

## 8. Keyboard Navigation

### 答题界面快捷键

| 快捷键    | 功能                 |
| --------- | -------------------- |
| `←` / `→` | 上/下一题            |
| `Space`   | 标记当前题           |
| `1-4`     | 选择选项 A-D（单选） |
| `Enter`   | 确认交卷（弹窗内）   |
| `Escape`  | 关闭弹窗             |

### Admin 后台

- `Tab` 导航所有可交互元素
- `Enter` 激活按钮/链接
- 弹窗打开时 focus trap
