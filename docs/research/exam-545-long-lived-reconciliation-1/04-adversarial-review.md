# #545 Fresh adversarial review

Fresh-context reviewer protocol: the reviewer received ONLY Issue #545, the #552
roadmap section, the #304 authority contract, the current diff, the evidence
docs/plans, and the tests — and attacked along the 10 mandated lines. Reviewer
verdict: **READY_FOR_HUMAN_REVIEW** (0 MAJOR). All MINOR findings below were
addressed before merge.

```json
{"severity":"MINOR","attack":8,"claim":"plans/base/ is labeled 'base = BASE master' (no hb index), but the committed S1 base plan uses the rejected candidate index attempt_interruption_events_org_occurred_hb_idx — a plan that cannot exist on BASE master","recommendation":"Re-capture base/q1-discovery-s1000.json on a pristine BASE-master schema (or move the with-index plan into an explicitly named idx-experiment set) and align the 01-query-plans.md S1 row before merge"}
{"severity":"MINOR","attack":8,"claim":"The index-rejection narrative's key step — 'index present, ANALYZE'd stats, planner still chooses Seq Scan + Sort' — has no committed artifact: after/q1-discovery-s50000.json (the cited seq-scan evidence, 57.5 ms) was captured on a cluster with NO index, and the only committed with-index plans show the planner TAKING the index (base/q1-after-idx-s50000.json, 125.7 ms; base/q1-discovery-s1000.json)","recommendation":"Commit one EXPLAIN with the index present AND healthy (ANALYZE'd) statistics showing the refusal; alternatively soften the wording to what the artifacts prove: even when taken, the index path (125.7 ms serial) is no better than seq+sort, so it fails the benefit bar regardless of planner choice"}
{"severity":"MINOR","attack":6,"claim":"The AFTER steady-tick figures cited in the evidence docs (S1 34 / S2 262 / S3 1,294 ms; whole 3-org loop up to 1,671 ms) are not traceable to the committed AFTER artifact, which records 29.4 / 215.3 / 1,058.4 ms and whole-loop 1,385/1,301 ms","recommendation":"Either commit the summary from the run the doc numbers came from, or re-cite the committed after/summary.json values; the 'no regression' conclusion survives either way (committed AFTER is faster than BASE)"}
{"severity":"MINOR","attack":6,"claim":"The index A/B dataset makes the candidate index look maximally useless: the synthetic table contains ONLY heartbeat-detected events (no restored/terminalized outcome rows, no operator incidents, no migration_backfill), so the org's history is ~100% of the table; real long-lived tables carry ≥2 events per episode plus non-heartbeat rows, lowering seq-scan selectivity","recommendation":"Documented honestly as one-dataset evidence; re-check the index decision at #550 with a composition-realistic table (detected + outcome events per episode) rather than treating this rejection as final"}
{"severity":"INFO","attack":4,"claim":"'The chunk union is exactly the single-probe result' (incidentRepo.ts comment) is a static-table claim: the chunked probe runs ⌈N/10k⌉ independent statements, so under a concurrent commit a later chunk can observe a row a single-statement snapshot would miss (and vice versa). Benign — an extra matching hit only converts a would-be idempotent replay into a skip, and the delivery path's in-transaction preReadOperationId remains the authoritative dedupe; C3 covers the race","recommendation":"Optionally narrow the comment to 'given the committed arbiter rows at call time'; no code change needed"}
{"severity":"INFO","attack":8,"claim":"The Q2 probe claim 'fully index-backed at every scale (22.9 ms server at 50k ids)' has no committed raw EXPLAIN artifact — plans/ contains only Q1 discovery plans plus summaries","recommendation":"Low risk (the probe filters the leading org column of the arbiter unique), but commit the Q2 EXPLAIN JSONs for completeness"}
{"severity":"INFO","attack":"other","claim":"T1/T2/T4/T7 labels referenced throughout the verdict are never defined in the committed evidence set (only T3/T5/T6/T8 get inline definitions); a fresh reader cannot reconstruct what T1/T2/T7 originally denoted beyond the inline C-test mapping","recommendation":"Add one line defining the T-number origin (e.g. the issue discussion or experiment protocol) or drop the T-labels in favor of the C-test names"}
```

```json
{"verdict":"READY_FOR_HUMAN_REVIEW","summary":"Production change (packages/db/src/repository/incidentRepo.ts chunked probe, OPERATION_PROBE_BATCH=10_000) is correct and authority-preserving: no time horizon, no second completion authority, no cursor/cross-call state, no starvation path, no scope creep into #554 territory, and it is a bind-protocol correctness fix — not a timeout/pool disguise (BEFORE_AFTER honestly reports wall time unchanged). All 10 attack lines checked: 1/2/3/5/9/10 clean; chunk-union≡single-probe verified against the arbiter unique (schema/pg.ts) including empty/duplicate/boundary cases; the 65,533 cliff verified in driver source (postgres.js connection.js, >=65534 params); new T3/chunk tests assert what the verdict claims and C1–C6 match. The index was genuinely not landed (no schema/migration diff) and its rejection is directionally supported, but the evidence set had four MINOR hygiene defects plus three INFO notes — none flips a conclusion."}
```

## Disposition of findings

| Finding | Disposition |
| --- | --- |
| MINOR 1 — contaminated `base/q1-discovery-s1000.json` | Evidence sets reorganized into `plans/base-master/` (verified pristine: s10000/s50000/summary/bind-cliff), `plans/idx-experiment/` (explicitly named single-dataset index A/B), `plans/chunked-probe/` (post-fix run). The contaminated S1 file moved out of base-master; 01-query-plans.md S1 row now states the raw S1 BASE file was overwritten and cites the live-run numbers. |
| MINOR 2 — missing with-index refusal artifact | The post-fix run WAS captured with the candidate index migration still present + explicit ANALYZE — `plans/chunked-probe/q1-discovery-s50000.json` is that refusal plan (parallel Seq Scan + Sort, 57.5 ms). Docs now cite it with the parallel-vs-serial caveat AND adopt the reviewer's softer, artifact-only argument: even when taken (125.7 ms serial), the index is no better than the no-index path, so it fails the benefit bar regardless of planner choice. |
| MINOR 3 — untraceable AFTER figures | Docs now cite exactly the committed `plans/chunked-probe/summary.json` (29.4 / 215.3 / 1,058.4 ms steady; whole loop 1,385 / 1,301 ms); BASE-vs-AFTER tables present ranges with the source of each endpoint named. |
| MINOR 4 — heartbeat-only table composition | Recorded in 01-query-plans.md and as Residual Unknown #4 in 03-final-verdict.md: the A/B understates the index's benefit; re-check at #550 with a composition-realistic dataset. |
| INFO — chunk-union snapshot nuance | The "committed at call time" narrowing was still an over-claim: under READ COMMITTED each chunk statement takes its own snapshot, so the union is not a single point-in-time result. Human review of PR #561 required the truthful statement, now in the incidentRepo.ts comment: observed matches are authoritative completion evidence that safely suppress delivery; a commit exposed to a later chunk but missed by an earlier snapshot is revalidated by the canonical delivery pre-read (replay / conflict / create); mismatched operationId occupancy stays a visible conflict. No code change — chunking carries no correctness dependency on snapshot consistency; the delivery pre-read remains the authoritative dedupe (C3 covers the race). |
| INFO — missing Q2 raw artifact | `plans/base-master/q2-probe-s1000.json` committed; S2/S3 raw files (~0.7/3.7 MB, overwhelmingly the placeholder list) documented as intentionally not committed. |
| INFO — undefined T-labels | 03-final-verdict.md now defines the T-matrix origin and each label inline. |

Reviewer-executed gates: `pnpm format:check` PASS, `@exam/db` typecheck PASS;
DB-backed suites not re-executed in the review environment (no PostgreSQL) — the
authoring environment ran them (packages/db 697/697, api C1–C6 + heartbeat +
incident suites, `pnpm verify:static`) as recorded in the PR body.
