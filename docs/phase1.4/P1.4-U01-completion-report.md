# P1.4-U01: UI Design System Baseline - Completion Report

## Summary

Extract shared constants, unify semantic color tokens, add ErrorBoundary, fix BrandProvider fallback, replace native `<select>` with shadcn Select, and remove dead `/exam/settings` link.

## Changes

### New Files

| File | Purpose |
|------|---------|
| `apps/web/src/lib/constants.ts` | Shared labels: `STATUS_LABELS`, `STATUS_VARIANT`, `TYPE_LABELS`, `TYPE_VARIANT`, `CONNECTION_STATUS_LABELS` |
| `apps/web/src/components/shared/ErrorBoundary.tsx` | Class-component error boundary wrapping the app |
| `apps/web/src/lib/constants.test.ts` | 7 tests covering all exported constants |
| `apps/web/src/components/shared/ErrorBoundary.test.tsx` | 5 tests: normal render, error catch, unknown error, reload button, reset |
| `apps/web/src/components/layout/BrandProvider.test.tsx` | 4 tests: fallback, custom value, remote load, fetch failure |

### Modified Files

| File | Change |
|------|--------|
| `apps/web/src/App.tsx` | Wrap with `<ErrorBoundary>` |
| `apps/web/src/components/layout/BrandProvider.tsx` | Fallback: `"Assessment Platform"` / `"Internal Assessment & Access Control"` |
| `apps/web/src/components/layout/ExamLayout.tsx` | Remove dead `/exam/settings` nav link |
| `apps/web/src/components/shared/ConnectionIndicator.tsx` | `bg-green-500` → `bg-success`, `bg-yellow-500` → `bg-warning`, `bg-red-500` → `bg-destructive`; labels from constants |
| `apps/web/src/components/exam/QuestionNav.tsx` | `bg-green-500` → `bg-success`, `bg-amber-*` → `bg-warning` semantic tokens |
| `apps/web/src/pages/admin/CandidateFieldsPage.tsx` | Native `<select>` → shadcn `<Select>` |
| `apps/web/src/pages/admin/ExamPage.tsx` | Remove local `statusLabels`/`statusVariant`; import from constants |
| `apps/web/src/pages/admin/ExamDetailPage.tsx` | Remove local `statusLabels`; import from constants |
| `apps/web/src/pages/admin/DashboardPage.tsx` | Remove local `statusLabels`; import from constants |
| `apps/web/src/pages/admin/QuestionPage.tsx` | Remove local `typeLabels`/`typeVariant`; import from constants |
| `apps/web/src/pages/admin/ExamCreatePage.tsx` | Remove local `typeLabels`; import from constants |
| `apps/web/src/pages/admin/QuestionImportPage.tsx` | Remove local `typeLabels`; import from constants |
| `apps/web/src/components/layout/layout.test.tsx` | Update branding assertions to new English fallback; remove dead `/exam/settings` link assertion |

## Acceptance Criteria

- [x] `statusLabels` has no local duplicate definitions
- [x] `bg-green-500` / `bg-yellow-500` / `bg-red-500` eliminated from components/
- [x] ErrorBoundary wraps App
- [x] `/exam/settings` dead link removed from ExamLayout
- [x] BrandProvider fallback contains no Chinese scenario-specific words
- [x] `pnpm typecheck` + `pnpm lint:copy` pass
- [x] Required tests created: `constants.test.ts`, `ErrorBoundary.test.tsx`, `BrandProvider.test.tsx`

## Verification

```shell
pnpm verify (format:check + lint + lint:copy + lint:arch + typecheck + test + coverage + build)
→ All passed. 322 tests passing, build successful.
```

## Known Limitations

1. `ErrorBoundary` reload test cannot spy on `window.location.reload` in jsdom (read-only property); test verifies button renders instead.
2. Bundle size warning for `index.js` (604 kB) — pre-existing, not introduced by this change.
