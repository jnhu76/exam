# Review Context Pack: P1.4-U01 — UI Design System Baseline & Settings Form Cleanup

## 1. PR Intent

- Primary goal: Extract shared UI constants, unify semantic color tokens, add ErrorBoundary, fix BrandProvider fallback, wire settings form to existing data, consolidate type sources.
- Original bug: Settings form always started empty; duplicate status/type mappings; hardcoded English branding; no error boundary; local DTOs diverging from contracts.
- Expected behavior: Form pre-filled with current settings; shared constants used everywhere; Chinese branding fallback; ErrorBoundary catches render errors; types from `@exam/domain` and `@exam/contracts`.
- Non-goals: Backend changes, new features, i18n framework, OrganizationSettings integration.

## 2. PR Boundary

### In Scope

- Shared constants with type guards and getters
- `ExamStatus`/`QuestionType` types from `@exam/domain`
- `SettingsFormValues` = `UpdateBrandingRequest` from `@exam/contracts`
- ErrorBoundary class component
- BrandProvider Chinese fallback
- Settings form `initialValues` prop + `reset()` pattern
- Empty string → `null` on settings save
- Semantic color tokens replacing hardcoded `bg-*` classes
- Dead link removal

### Out of Scope

- Backend API, database, exam engine, auth, new pages/routes, OrganizationSettings

### Allowed Files

23 files as listed in boundary doc `docs/ai/pr-boundaries/p1.4-u01.md`

### Forbidden Expansion

Do not refactor exam engine, auth, database, API routes, backend packages, or add new features.

### Boundary Drift Suspected

No. All files belong to job card scope.

## 3. Changed Surface

### Production files

| File | Role in this PR | Review focus |
|---|---|---|
| `apps/web/src/lib/constants.ts` | Shared labels + variants + type guards + getters | Types imported from `@exam/domain`, `Record<string,...>` preserved |
| `apps/web/src/components/shared/ErrorBoundary.tsx` | App-level error boundary | `handleReset` calls `reload()`, no stale `setState` |
| `apps/web/src/components/layout/BrandProvider.tsx` | Branding context provider | Fallback is Chinese, `loadRemote` fetches from API |
| `apps/web/src/components/settings/PlatformSettingsForm.tsx` | Settings editing form | `SettingsFormValues` = `UpdateBrandingRequest` from contracts |
| `apps/web/src/pages/admin/SettingsPage.tsx` | Settings page orchestrator | Uses `UpdateBrandingRequest` from contracts, passes `initialValues` |
| `apps/web/src/pages/admin/DashboardPage.tsx` | Dashboard with StatusBadge | Uses `STATUS_VARIANT` from constants |
| `apps/web/src/App.tsx` | App root | Wrapped with `ErrorBoundary` |

### Test files

| File | What it proves | Possible blind spot |
|---|---|---|
| `apps/web/src/lib/constants.test.ts` | Constants values, type guards, getters | — |
| `apps/web/src/components/shared/ErrorBoundary.test.tsx` | Error catch, reload button, reload call | jsdom `window.location.reload` spy, global stub cleanup |
| `apps/web/src/components/layout/BrandProvider.test.tsx` | Fallback, custom, remote, fetch failure | `afterEach` cleanup for `fetch` stub |
| `apps/web/src/components/layout/layout.test.tsx` | Branding assertions use Chinese | — |
| `apps/web/src/pages/admin/SettingsPage.test.tsx` | Settings page renders form with loaded data | — |
| `apps/web/src/components/settings/PlatformSettingsForm.test.tsx` | Form fills from initialValues, updates on prop change | — |

## 4. Call Chain / Data Flow

```text
User visits /admin/settings
  -> SettingsPage loads settings via api.get() (typed as UpdateBrandingRequest)
  -> passes settings as initialValues to PlatformSettingsForm
  -> PlatformSettingsForm.useEffect resets form with initialValues
  -> User edits fields, submits
  -> SettingsPage.handleSave maps empty strings to null
  -> api.patch() sends filtered payload
  -> window.dispatchEvent("branding:refresh")
  -> BrandProvider.fetch re-fetches branding from API
```

| Step | Contract to verify | Failure mode |
|---|---|---|
| `api.get` | Returns shape matching `UpdateBrandingRequest` | Form shows stale or empty data |
| `useEffect` + `reset()` | Re-initializes form on prop change | Form stays empty after load |
| `handleSave` empty→null | Empty string sent as `null` | Backend receives `""` instead of `null` |
| `STATUS_VARIANT[key]` | Returns valid variant or undefined | Unknown status → crash or wrong badge |
| `ExamStatus`/`QuestionType` from domain | Types match enum values | Compile error or runtime mismatch |

## 5. Contract Invariants

- `ExamStatus`/`QuestionType` types sourced from `@exam/domain` (not local)
- `SettingsFormValues` = `UpdateBrandingRequest` from `@exam/contracts`
- `STATUS_LABELS` / `STATUS_VARIANT` keys are `Record<string, ...>` for backward compatibility
- `PlatformSettingsForm` accepts optional `initialValues` prop
- `SettingsPage.handleSave` maps `""` → `null` for all fields
- `ErrorBoundary.handleReset` calls only `window.location.reload()` (no `setState`)
- `BrandProvider` fallback is Chinese: "考试平台" / "内部考核与准入控制"

## 6. Targeted Risk Checklist

### Form State

- [ ] `initialValues` prop triggers `reset()` via `useEffect`
- [ ] Empty string → `null` on save is consistent
- [ ] `UpdateBrandingRequest` schema matches form fields exactly

### Shared Constants & Types

- [ ] `ExamStatus`/`QuestionType` imported from `@exam/domain` — values match
- [ ] `Record<string, ...>` preserved for backward compat
- [ ] Type guards don't break callers using string keys
- [ ] `DashboardPage.StatusBadge` uses `STATUS_VARIANT[status] ?? "outline"`

### ErrorBoundary

- [ ] No `setState` before `reload()` (removed)
- [ ] Test spies on `window.location.reload`
- [ ] `vi.unstubAllGlobals()` cleans up location stub

### Test Isolation

- [ ] `afterEach` unstub globals in BrandProvider tests
- [ ] ErrorBoundary test cleans up both `console.error` spy and global location stub

## 7. Regression Proof Required

| Original failure | Required test | Required assertion |
|---|---|---|
| Form always starts empty | `SettingsPage.test.tsx` | Form renders with `toHaveValue("Test Platform")` |
| PlatformSettingsForm not reset on prop change | `PlatformSettingsForm.test.tsx` | `rerender` with new `initialValues` → form updates |
| Duplicate status mappings | `constants.test.ts` | All status/type variants valid |
| No error boundary | `ErrorBoundary.test.tsx` | Error catch, reload button, reload spy |
| English branding fallback | `BrandProvider.test.tsx` | "考试平台" / "内部考核与准入控制" |

## 8. Reviewer Decision Rules

Reviewer findings must use only:

- `Fix now`
- `Add test now`
- `Defer issue`
- `Investigate`
- `Reject`

Rules:

- Current PR introduced correctness / security / data integrity bug -> `Fix now`
- Current PR introduced behavior or error contract without tests -> `Add test now`
- Real issue outside current PR boundary -> `Defer issue`
- Insufficient evidence -> `Investigate`
- False positive or not applicable -> `Reject`

## 9. Required Reviewer Output

| 文件/位置 | 问题 | 严重程度 | 当前 PR 引入？ | 是否 blocking | 处理决策 | 最小修复 |
|---|---|---|---|---|---|---|

## 10. Handoff

Now use superpowers requesting-code-review with this Review Context Pack.

Do not use the full conversation history as review context.
Do not expand beyond the PR Boundary.
If a real issue is outside the PR boundary, classify it as Defer issue.
If the issue is inside the boundary and affects correctness, tests, security, or data integrity, classify it as Fix now or Add test now.
