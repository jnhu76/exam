# Follow-up Job: <job-name>

## 1. Source

- Origin PR / Job: `<source>`
- Origin review: `<review source>`
- Related boundary: `docs/ai/pr-boundaries/<source>.md`

## 2. Problem

`<describe the confirmed issue>`

## 3. Why Deferred

This was not fixed in the origin PR because:

- `<reason>`

## 4. Scope

In scope:

- `<path / behavior>`

Out of scope:

- `<excluded work>`

## 5. Acceptance Criteria

- [ ] `<criterion>`
- [ ] `<criterion>`

## 6. Suggested Review Profiles

- `docs/ai/review-profiles/<profile>.md`
