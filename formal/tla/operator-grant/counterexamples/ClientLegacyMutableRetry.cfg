\* REC-I4-F1 — Expected-counterexample configuration.
\*
\* Legacy defect: retry reuses operationId but mutates the frozen payload.
\*
\* Expected: FrozenCommandImmutable is violated.
\*   After retry, the frozen command no longer matches the shared authority.

SPECIFICATION Spec

INVARIANT FrozenCommandImmutable

CONSTANTS
  Tabs = {Tab1, Tab2}
  CmdIds = {C1, C2, C3, C4}
  Ops = {O1, O2}
  Payloads = {P1, P2}
  C1 = C1
  C2 = C2
  C3 = C3
  C4 = C4
  O1 = O1
  O2 = O2
  P1 = P1
  P2 = P2
  LegacyPerTabPending = FALSE
  LegacyNewIdentityAfterLoss = FALSE
  LegacyMutableRetry = TRUE
  LegacyTerminalAsGranted = FALSE
